# 生产环境配置 - 使用 GHCR 镜像
# 用法: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    image: ghcr.io/ldy2alen/fastapi1/backend:latest
    build: !reset null

  frontend:
    image: ghcr.io/ldy2alen/fastapi1/frontend:latest
    build: !reset null
